version: "3.9"

services:
  api:
    build:
      context: .
      dockerfile: docker/Dockerfile
    ports:
      - "8000:8000"
    env_file:
      - .env
    environment:
      - APP_NAME=Kirov Security API Suite
      - APP_VERSION=1.0.0
      - DEBUG=false
